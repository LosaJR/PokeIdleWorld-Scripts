# Identidad de Tampermonkey

Las actualizaciones conservan el mismo `@name` y `@namespace` de cada script instalado. Esta pareja identifica el userscript y evita crear copias duplicadas durante la instalación inicial desde GitHub Pages.
