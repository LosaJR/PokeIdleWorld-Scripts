name: Validar scripts

on:
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Ensamblar carga verificada
        run: node scripts/assemble-upload.mjs
      - name: Validar y generar
        run: npm run check
